#include <iostream>
#include <cmath>

using namespace std;

double func1(int x)
{
	return exp(x) + x - 4;
}

double func2(int x)
{
	return pow(x, 2) - 4;
}

double metod1(double(*func)(int), )

int main() {
	setlocale(LC_ALL, "ru");
	float e = 0.001;
	int a, b, x = 2;

	cout << "Введите число a: "; // Вывод на экран предложения ввести число a
	cin >> a; // Считываем с клавиатуры символ a
	cout << "Введите число b: "; // Вывод на экран предложения ввести число a
	cin >> b; // Считываем с клавиатуры символ a


	return 0;
}